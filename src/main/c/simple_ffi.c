#include <stdint.h>

// 最简单的函数 - 只返回输入
int64_t simple_identity(int64_t x) {
    return x;
}

// 简单加法
int64_t simple_add(int64_t a, int64_t b) {
    return a + b;
}

// 简单除法
int64_t simple_divide(int64_t a, int64_t b) {
    return a / b;
}
